{
  "name": "swaps",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "tools/*",
    "app"
  ],
  "scripts": {
    "test": "npm run test -w @swaps/engine -w @swaps/sync",
    "sync": "npm run sync -w @swaps/sync",
    "build": "tsc -b packages/engine tools/sync"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  },
  "overrides": {
    "react": "18.3.1",
    "react-native": "0.76.5",
    "expo": "~52.0.0"
  }
}
